<UserControl x:Class="TAM.Views.PurchaseOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:TAM.Models">
    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Purchase Orders" Style="{StaticResource PageTitle}"/>
            <Button Grid.Column="1" Content="+ New Purchase Order" Command="{Binding AddCommand}"
                    Style="{StaticResource PrimaryButton}" VerticalAlignment="Center"/>
        </Grid>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <Border.Effect>
                <DropShadowEffect Color="#10000000" BlurRadius="4" ShadowDepth="1"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource FormTextBox}" Width="220"/>
                <TextBlock Text="Status:" VerticalAlignment="Center" Margin="16,0,8,0" Foreground="{StaticResource TextSecondary}"/>
                <ComboBox SelectedItem="{Binding FilterStatus}" Style="{StaticResource FormComboBox}" Width="140">
                    <ComboBox.Items>
                        <x:Null/>
                        <models:POStatus>Draft</models:POStatus>
                        <models:POStatus>PartiallyInward</models:POStatus>
                        <models:POStatus>Completed</models:POStatus>
                        <models:POStatus>Cancelled</models:POStatus>
                    </ComboBox.Items>
                </ComboBox>
                <Button Content="Edit" Command="{Binding EditCommand}" Style="{StaticResource SecondaryButton}" Margin="16,0,0,0"/>
                <Button Content="Convert to Inward" Command="{Binding ConvertCommand}" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"/>
                <Button Content="Delete" Command="{Binding DeleteCommand}" Style="{StaticResource DangerButton}" Margin="8,0,0,0"/>
                <Button Content="Clear" Command="{Binding ClearFilterCommand}" Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="#10000000" BlurRadius="4" ShadowDepth="1"/>
            </Border.Effect>
            <DataGrid ItemsSource="{Binding Orders}" SelectedItem="{Binding Selected}"
                      Style="{StaticResource AppDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PO Number" Binding="{Binding PONumber}" Width="130"/>
                    <DataGridTextColumn Header="Vendor" Width="160">
                        <DataGridTextColumn.Binding>
                            <Binding Path="VendorId" Converter="{StaticResource VendorName}"/>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="Status" Width="130" SortMemberPath="Status">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="10" Padding="8,3" HorizontalAlignment="Left"
                                        Background="{Binding Status, Converter={StaticResource POStatusColor}}">
                                    <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Items" Binding="{Binding Items.Count}" Width="60"/>
                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                    <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat='dd-MM-yyyy'}" Width="100"/>
                    <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAt, StringFormat='dd-MM-yyyy'}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
